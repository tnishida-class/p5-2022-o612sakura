let x,y;
function setup(){
  createCanvas(windowWidth, windowHeight);
 x=width/10
 y=width*9/10
}
function draw(){
  background(160, 192, 255);
  ellipse(x,y,20);
  if(keyIsDown(RIGHT_ARROW)){x=x+1}
  if(keyIsDown(LEFT_ARROW)){x=x-1}
}